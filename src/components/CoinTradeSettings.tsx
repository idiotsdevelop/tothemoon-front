import styled from 'styled-components'

const Wrapper = styled.div`
  min-width: 30vw;
  background-color: #121721;
  border-radius: 7px;
  margin-left: 1vw;
`

const CoinTradeSettings = () => {
  return <Wrapper>자동매매 설정</Wrapper>
}

export default CoinTradeSettings
